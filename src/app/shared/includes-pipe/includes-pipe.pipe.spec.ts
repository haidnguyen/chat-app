import { IncludesPipe } from './includes-pipe.pipe';

describe('IncludesPipePipe', () => {
  it('create an instance', () => {
    const pipe = new IncludesPipe();
    expect(pipe).toBeTruthy();
  });
});
